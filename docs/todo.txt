Current todo:
- Reports init:
    - For start
        create report button redirects to /report-builder/ with current params for the sales
        u get all the sales for those params and return data for those sales

        save that data + file name + remarks to a pdf and db entry
            name for a file + email make unique name (cant have the same report name for the same user)
    - Change sales api endpoint to be ble to get all data in 1 request
    - Pass all Sales(all pages for selected filters) to Report Creation View when press Create Report button
    - Reports viz (use D3.js):
        significant sales KPIs and metrics(turnover, net margin, quantity sold) (for company)
        total sales volume (for company)
        net/gross sales (for company)
        KPI percentage comparisons between previous and selected year reports (for company)


- Refactor for better readability after done with Sales Filtering and basic Reports repr.
- Sales filter
    - Filter by
        min/max Rating
	    min/max region sales
	- Support for multiple filters of the same type eg.: genre=action,racing
- Sales
    - Sale Editing
    - Only see Sale Delete/Edit if admin
- Users
    - Add password Validation


Backend:
- Reports
    Model
    List/Detail endpoints
    save pdf file in db/storage for history of user reports
- Profiles
    Model
    Profile endpoint


Frontend:
- Reports
    Visualization for currently selected Sales
    Download as pdf
- Users
    userContext
    My profile View
    Reports history in Profile
    Add 'Remember me' on signin


Bugs/Fixes:
- Fix Field names in table
- Fix hardcoded css
- Improve performance where possible by removing Box or other MUI elements and replace it with normal elements+css
- Rework Header to look better on smaller screen
- Check: if creating through social-auth, dont need passwords
- Fix alert messages(better parsing)
- Error handling with alerts for all errors (possibly make Alert global?)
